#!/usr/bin/env bash
set -euo pipefail

QQ_CODE_TRIAL_ROOT="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd -P)"
export QQ_CODE_TRIAL_ROOT
exec node "$QQ_CODE_TRIAL_ROOT/lib/qq-code-trial.mjs" "$@"
